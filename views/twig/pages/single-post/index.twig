{% extends 'structure/html.twig' %}

{% block content %}
  <div class="container">
    <div class="row">
      <div class="col-12">
        <h1>{{ post.title }}</h1>
        {{ post.content }}
        {{ function('comments_template') }}
      </div>
    </div>
  </div>
{% endblock %}
